<view class="empty">
  <view class="empty-icon">{{icon}}</view>
  <view class="empty-text">{{text}}</view>
  <view wx:if="{{showAction}}" class="empty-action" bindtap="onAction">{{actionText}}</view>
</view>
